# App
PORT=3000
NODE_ENV=development

# Database
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=user_sb
POSTGRES_PASSWORD=password_sb
POSTGRES_DB=smart_buffet_db

# Optional DATABASE_URL override (auto-built from above values if unset)
#DATABASE_URL="postgresql://user_sb:password_sb@localhost:5432/smart_buffet_db?schema=public"